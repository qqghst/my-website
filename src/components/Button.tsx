import React from 'react';

const Button = () => {
    return (
        <button type='button' className={`text-[40px] text-white`}>
            click me
        </button>
    );
};

export default Button;